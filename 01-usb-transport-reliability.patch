--- a/r8152.c	2025-11-28 17:15:54.000000000 +0200
+++ b/r8152.c	2026-04-09 17:00:00.000000000 +0300
@@ -1150,22 +1150,30 @@
 int get_registers(struct r8152 *tp, u16 value, u16 index, u16 size, void *data)
 {
 	int ret;
+	int i;
 	void *tmp;

 	tmp = kmalloc(size, GFP_KERNEL);
 	if (!tmp)
 		return -ENOMEM;

-	ret = usb_control_msg(tp->udev, tp->pipe_ctrl_in,
-			      RTL8152_REQ_GET_REGS, RTL8152_REQT_READ,
-			      value, index, tmp, size, USB_CTRL_GET_TIMEOUT);
-	if (ret < 0)
-		memset(data, 0xff, size);
-	else
-		memcpy(data, tmp, size);
+	for (i = 0; i < 3; i++) {
+		ret = usb_control_msg(tp->udev, tp->pipe_ctrl_in,
+				      RTL8152_REQ_GET_REGS, RTL8152_REQT_READ,
+				      value, index, tmp, size,
+				      USB_CTRL_GET_TIMEOUT);
+		if (ret >= 0) {
+			memcpy(data, tmp, size);
+			goto out;
+		}
+		if (ret == -ENODEV || ret == -ESHUTDOWN)
+			break;
+		usleep_range(1000, 2000);
+	}
+	memset(data, 0xff, size);

+out:
 	kfree(tmp);
-
 	return ret;
 }

@@ -1173,18 +1181,26 @@
 int set_registers(struct r8152 *tp, u16 value, u16 index, u16 size, void *data)
 {
 	int ret;
+	int i;
 	void *tmp;

 	tmp = kmemdup(data, size, GFP_KERNEL);
 	if (!tmp)
 		return -ENOMEM;

-	ret = usb_control_msg(tp->udev, tp->pipe_ctrl_out,
-			      RTL8152_REQ_SET_REGS, RTL8152_REQT_WRITE,
-			      value, index, tmp, size, USB_CTRL_SET_TIMEOUT);
+	for (i = 0; i < 3; i++) {
+		ret = usb_control_msg(tp->udev, tp->pipe_ctrl_out,
+				      RTL8152_REQ_SET_REGS, RTL8152_REQT_WRITE,
+				      value, index, tmp, size,
+				      USB_CTRL_SET_TIMEOUT);
+		if (ret >= 0)
+			break;
+		if (ret == -ENODEV || ret == -ESHUTDOWN)
+			break;
+		usleep_range(1000, 2000);
+	}

 	kfree(tmp);
-
 	return ret;
 }

@@ -1360,7 +1376,7 @@
 	int ret;

 	ret = generic_ocp_read(tp, index, sizeof(data), &data, type);
-	WARN_ON_ONCE(ret != sizeof(data));
+	WARN_ON_ONCE(ret != sizeof(data) && ret >= 0);

 	*value = __le32_to_cpu(data);

@@ -1374,7 +1390,7 @@

 	ret = generic_ocp_write(tp, index, BYTE_EN_DWORD, sizeof(tmp), &tmp,
 				type);
-	WARN_ON_ONCE(ret != sizeof(tmp));
+	WARN_ON_ONCE(ret != sizeof(tmp) && ret >= 0);
 	return ret;
 }

@@ -1390,7 +1406,7 @@
 	byen <<= shift;

 	ret = generic_ocp_read(tp, index, sizeof(tmp), &tmp, type | byen);
-	WARN_ON_ONCE(ret != sizeof(tmp));
+	WARN_ON_ONCE(ret != sizeof(tmp) && ret >= 0);

 	data = __le32_to_cpu(tmp);
 	data >>= (shift * 8);
@@ -1420,7 +1436,7 @@
 	tmp = __cpu_to_le32(data);

 	ret = generic_ocp_write(tp, index, byen, sizeof(tmp), &tmp, type);
-	WARN_ON_ONCE(ret != sizeof(tmp));
+	WARN_ON_ONCE(ret != sizeof(tmp) && ret >= 0);

 	return ret;
 }
@@ -1435,7 +1451,7 @@
 	index &= ~3;

 	ret = generic_ocp_read(tp, index, sizeof(tmp), &tmp, type);
-	WARN_ON_ONCE(ret != sizeof(tmp));
+	WARN_ON_ONCE(ret != sizeof(tmp) && ret >= 0);

 	data = __le32_to_cpu(tmp);
 	data >>= (shift * 8);
@@ -1465,7 +1481,7 @@
 	tmp = __cpu_to_le32(data);

 	ret = generic_ocp_write(tp, index, byen, sizeof(tmp), &tmp, type);
-	WARN_ON_ONCE(ret != sizeof(tmp));
+	WARN_ON_ONCE(ret != sizeof(tmp) && ret >= 0);

 	return ret;
 }
@@ -1883,7 +1899,7 @@
 {
 	int i, ret;

-	for (i = 0; i < 10; i++) {
+	for (i = 0; i < 50; i++) {
 		u32 ocp_data;

 		ret = ocp_read_word(tp, MCU_TYPE_USB, USB_ADV_CMD, &ocp_data);
@@ -1894,7 +1910,7 @@
 		usleep_range(1000, 2000);
 	}

-	if (i == 10)
+	if (i == 50)
 		ret = -ETIMEDOUT;

 	return ret;
@@ -2379,6 +2395,22 @@
 			netif_warn(tp, rx_err, netdev,
 				   "maybe reset is needed?\n");
 		break;
+	case -EOVERFLOW:
+		if (net_ratelimit())
+			netif_warn(tp, rx_err, netdev,
+				   "Rx overflow\n");
+		break;
+	case -EPIPE:
+		if (net_ratelimit())
+			netif_warn(tp, rx_err, netdev,
+				   "Rx endpoint halted, resetting\n");
+		usb_queue_reset_device(tp->intf);
+		return;
+	case -EILSEQ:
+		if (net_ratelimit())
+			netif_warn(tp, rx_err, netdev,
+				   "Rx CRC/bitstuff error\n");
+		break;
 	default:
 		if (net_ratelimit())
 			netif_warn(tp, rx_err, netdev,
@@ -3165,6 +3197,17 @@
 		spin_unlock(&tx_queue->lock);
 	}

+	if (unlikely(agg->skb_num == 0)) {
+		unsigned long flags;
+
+		spin_lock_irqsave(&tp->tx_lock, flags);
+		list_add_tail(&agg->list, &tp->tx_free);
+		spin_unlock_irqrestore(&tp->tx_lock, flags);
+
+		ret = 0;
+		goto out_tx_fill;
+	}
+
 	netif_tx_lock(netdev);

 	if (netif_queue_stopped(netdev) &&
