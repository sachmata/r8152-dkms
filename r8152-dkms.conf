# Blacklist in-kernel drivers that conflict with the out-of-tree r8152 module.
# The out-of-tree module installed to /updates/ has priority via depmod, but
# these blacklist entries prevent the in-kernel modules from being auto-loaded
# by udev before depmod ordering takes effect.
blacklist r8152
blacklist r8153_ecm
blacklist cdc_ncm
